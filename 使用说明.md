# vsdx-gen 技能包 · 使用说明

这个技能用纯 Python 标准库生成 Open Packaging 单页 `.vsdx`。生成器同时面向 Microsoft Visio
与 draw.io 的 VSDX 导入器。已实测 Visio 2016 桌面版与 draw.io 31.1.5；其他 Visio/网页版与
其他 draw.io 版本未承诺。图片或手绘内容由模型先重建为 JSON，脚本不直接做 OCR 或矢量化。

## 功能

- Mermaid、结构化描述或图片重建结果 → 可编辑 `.vsdx`
- 17 种内置形状与 15 种严格自定义几何行类型
- 颜色、渐变、虚线、透明度、旋转、字体、粗斜下划线、对齐、箭头和折线路由
- 输入、ZIP/XML 结构、连接端点和布局几何校验
- 可选的本地 draw.io + Playwright + Firefox 导入回归

## 命令

命令不依赖当前工作目录。先把本文件所在目录记为 `<skill-dir>`，把 draw.io webapp 目录记为
`<drawio-webapp-dir>`，再使用绝对路径：

```powershell
python "<skill-dir>\scripts\vsdx_gen.py" "<skill-dir>\examples\login-flow.json" "<output.vsdx>"
```

导入验证是可选集成工具，需要本地 draw.io webapp、Playwright 和 Firefox：

在终端 A 安装依赖、启动服务并保持运行：

```powershell
python -m pip install playwright
python -m playwright install firefox
Set-Location "<drawio-webapp-dir>"
python -m http.server 8080 --bind 127.0.0.1
```

依赖安装可以提前完成；终端 A 启动服务后，在终端 B 执行导入：

```powershell
python "<skill-dir>\scripts\test_import.py" "<output.vsdx>" "<result.drawio>" --expect-nodes 2 --expect-edges 1
```

导入成功后，再对刚生成的 XML 做布局检查：

```powershell
python "<skill-dir>\scripts\verify_layout.py" "<result.drawio>" --expect-nodes 2 --expect-edges 1
```

布局验证新增页面尺寸契约：`--expect-page-width-in W --expect-page-height-in H` 按源 JSON 页面
尺寸严格校验（容差 1px）；仅当显式加 `--allow-tiled-paper` 时才允许 draw.io 默认纸张与源尺寸
不一致，且内容边界仍按源尺寸检查。节点四角（含旋转）、边路径点和标签中心必须落在页面内
（0.5px epsilon）。

批量导入会为每个输入创建安全且不冲突的 XML/截图文件名：

```powershell
python "<skill-dir>\scripts\batch_import.py" "<one.vsdx>" "<two.vsdx>" --output-dir "<result-dir>"
```

单文件导入支持 `--url`、`--timeout`、`--screenshot`、`--expect-nodes`、`--expect-edges`；提供哪个
`--expect-*` 就断言对应的精确数量。默认 URL 是 `http://127.0.0.1:8080`，截图与输出 XML 同目录。
布局验证使用 ElementTree，检查节点重叠、边穿过第三方节点、
未绑定边、源/目标重入、标签中心遮挡和页面越界；边界接触按 0.5px epsilon 处理。

Windows 上若出现 `Browser.new_page: Cannot read properties of undefined (reading '_page')`，
导入工具会自动重试并在最后一次为 Firefox 设置 `MOZ_DISABLE_CONTENT_SANDBOX=1`。

**Visio 桌面验收**（本机 Visio 2016）：

```powershell
powershell.exe -NoProfile -STA -ExecutionPolicy Bypass -File "<skill-dir>\scripts\run_visio_acceptance.ps1" -VsdxPath "<output.vsdx>" -ExpectedPages 1 -ExpectedShapes 39
```

输出一行 JSON：`exit_code` 0=通过，1=兼容性失败，2=参数/无 COM/非 STA 环境失败。门禁以
无头 COM 直接打开原件（不做副本——本机 Visio 2016 打开字节级副本会卡死），不保存关闭，
并用打开前后的 SHA-256 确认源文件未被改动。

## JSON 要点

- 单位是英寸，坐标 Y 向上，`x`/`y` 是节点中心；页面默认 8.5×11
- 顶层只能有 `page`、`nodes`、`edges`；`nodes` 非空，`edges` 可省略
- 所有数字必须有限；节点 `id/x/y/w/h` 必填；边 `from/to` 必须引用现有节点
- 边可选 `routing`：`auto`（默认，斜向边自动正交折线，穿过其他节点时退回直线）/ `straight` / `elbow`（强制折线）；显式 `points` 优先
- 自定义 `geometry` 首行必须是 `MoveTo`；共 15 种行类型，`Rel*` 的 X/Y 为 0..1；`type` 可省略，省略时形状元数据按 `rect`，轮廓仍以 `geometry` 为准
- 边的 `points` 只表示中间路由点，源/目标锚点由生成器自动补齐并写入 `<Connects>`

完整字段、形状列表、Mermaid 映射和维护陷阱见 `SKILL.md`。

## 文件结构

```text
vsdx-gen/
├── SKILL.md
├── 使用说明.md
├── scripts/
│   ├── vsdx_gen.py
│   ├── test_import.py
│   ├── verify_layout.py
│   └── batch_import.py
├── tests/
└── examples/
    ├── login-flow.json
    ├── shapes-showcase.json
    ├── ecommerce-order-distribution.json
    └── stress-flow.json
```

## 依赖和退出码

- 生成器和布局验证只使用 Python 标准库，可离线运行
- draw.io 导入验证额外需要本地 webapp、Playwright 和 Firefox
- 生成器：`0` 成功，`1` 结构/语义校验失败，`2` 输入/参数/文件错误
- 布局验证：`0` 通过，`1` 发现布局问题，`2` 参数/文件/XML 错误
- 单文件导入：`0` 通过，`1` 服务/浏览器/导入/验证失败，`2` 参数/依赖/文件错误
- Visio 验收：`0` 通过，`1` 打开/计数/哈希等兼容性失败，`2` 参数/COM 不可用/非 STA 环境
- 批量导入：任一文件失败返回 `1`；参数或启动阶段输出目录错误返回 `2`

## 验收边界

本技能只覆盖单页 VSDX、普通节点/边、样式和自定义几何；不覆盖泳道、容器、主题、位图嵌入或 Visio
master 扩展。已实测：Visio 2016 桌面版无头 COM 门禁打开（1 页/39 形状，源哈希未变）与
draw.io 31.1.5 四案例导入；Visio 必需部件 `visio/windows.xml` 已纳入生成与严格校验。
Office 365 网页上传因账号/许可环境被排除，未纳入验收；其他 Visio/draw.io 版本未实测时，
交付说明必须如实标注。
